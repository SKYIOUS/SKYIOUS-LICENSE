name: License Check
on: [pull_request]  # Runs when code is submitted

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4  # Get the code
      - uses: license-eye/action@v2  # Scan licenses
        with:
          config: .licensee.yml  # Use our config
